# Option Menu Extract

Standalone copy of the options menu from the main project.

It is packaged without the original background image so you can render it as an overlay inside the game.

Build:

```bash
make -C option
```

Run:

```bash
./option/option_menu
```
